# SC is created by k8-admins
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: roboshop-ebs-sc
reclaimPolicy: Delete # Retain 
provisioner: ebs.csi.aws.com  # drivers must be installed 
volumeBindingMode: WaitForFirstConsumer