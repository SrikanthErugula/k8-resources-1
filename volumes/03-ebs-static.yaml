# Creating PV is K8 admin job
apiVersion: v1
kind: PersistentVolume
metadata:
  name: ebs-static-pv
spec:
  accessModes:
  - ReadWriteOnce   # https://kubernetes.io/docs/concepts/storage/persistent-volumes/  type readwrite we have 3 optins
  persistentVolumeReclaimPolicy: Delete # Retain # retain means don't delete data in disk, Recycle means delete data don't delete disk.  #Delete means if POD deleted then  delete the disk
  capacity:
    storage: 15Gi
  csi:
    driver: ebs.csi.aws.com
    fsType: ext4  # latest,  ask web for more, fs means file syatem
    volumeHandle: vol-038b6cf22e8c51832 # this created by manul in AWS volume lo that is it 

---
# Creating a PVC code is here 
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ebs-static-pvc
spec:
  storageClassName: "" # Empty string must be explicitly set otherwise default StorageClass will be set
  volumeName: ebs-static-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 15Gi  # 50gi see error   # 15 kante ekkuva iste pod status is pending  and events lo error like PV less so u need to create under PV

---
# Now we are adding above PVC to POD level 

apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    purpose: ebs-static-def
    practice: ebs # created by me
spec:
  containers:
  - name: nginx
    image: nginx
    volumeMounts:
    - name: nginx-html
      mountPath: /usr/share/nginx/html  # this ex purpose but real time lo code in git only 
  nodeSelector:
    topology.kubernetes.io/zone: us-east-1b  # we selet node bcz we have multiplr nodes 
  volumes:
  - name: nginx-html
    persistentVolumeClaim:
      claimName: ebs-static-pvc  # Name should be same In PVC spec lo above

---
#Service code 
apiVersion: v1
kind: Service
metadata:
  name: nginx-ebs-static-sv
spec:
  type: LoadBalancer
  selector:
    purpose: ebs-static-def
    practice: ebs # created by me, this key and value, bcz we can add many 
  ports:
    - protocol: TCP
      port: 80 # service port
      targetPort: 80 # container port
      nodePort: 30007 